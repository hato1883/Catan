package io.github.hato1883.api.events.ui;

/**
 * Enum for supported batch types in the UI rendering system.
 */
public enum BatchType {
    SPRITE,
    POLYGON,
    SHAPE
    // Add more as needed
}

